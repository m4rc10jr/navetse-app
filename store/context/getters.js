const route = (state) => state.data.route;

const item = (state) => state.data.item;

const api = state => state.data.api;

const filter = state => state.filter;

export default { route, item, api, filter }
